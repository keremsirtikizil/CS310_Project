import 'package:flutter/material.dart';

class FinancingPage extends StatelessWidget {
  const FinancingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Financing Page')),
    );
  }
}
